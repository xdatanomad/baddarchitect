---
import { site } from "@/lib/site";
---

<footer class="site-footer">
  <div class="shell footer-grid">
    <div>
      <strong>
        <span class="sr-only">{site.name}</span>
        <span aria-hidden="true" class="brand-wordmark">
          <span class="brand-wordmark__small">Badd</span>
          <span class="brand-wordmark__divider">|</span>
          <span>Architect</span>
        </span>
      </strong>
      <p>
        A practical blueprint for AI architects and leaders moving from scattered experimentation to governed,
        production-grade AI adoption.
      </p>
    </div>
    <nav aria-label="Footer navigation">
      <a href="/stages/">Journey</a>
      <a href="/guides/">Guides</a>
      <a href="/blog/">Blog</a>
      <a href="/about/">About</a>
      <a href="/rss.xml">RSS</a>
    </nav>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--line);
    padding: 38px 0;
  }

  .footer-grid {
    align-items: start;
    display: grid;
    gap: 24px;
    grid-template-columns: minmax(0, 1.5fr) minmax(min(220px, 100%), 0.7fr);
  }

  p {
    color: var(--ink-soft);
    margin: 8px 0 0;
    max-width: 520px;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    gap: 12px 18px;
    justify-content: flex-end;
  }

  nav a {
    color: var(--ink);
    font-family: var(--sans);
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
  }

  nav a:hover {
    color: var(--accent);
  }

  @media (max-width: 720px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }

    nav {
      justify-content: flex-start;
    }
  }
</style>
